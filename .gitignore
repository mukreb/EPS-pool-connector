# Secrets - never commit
.env

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# This checkout currently also contains a local virtualenv at its root.
# Anchored to the repo root so this doesn't also swallow unrelated lib/ (etc.)
# directories elsewhere in the tree, e.g. prototypes/*/lib/.
/bin/
/include/
/lib/
/pyvenv.cfg
.venv/
venv/

# Node / Homey
node_modules/
.homeybuild/
env.json
*.log
